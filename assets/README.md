# Media files

Add the 5 MB opening video here as `wedding-film.mp4`.

Optional replacements:

- `hero-poster.jpg` — still image shown before the video loads.
- `reception.jpg` — image used on the reception card.

The site has graceful colour fallbacks when these files are not present.
